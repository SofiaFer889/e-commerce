{{> header}}

<div>
    <h3>productos de un carrito</h3>
    {{#each cart.products}}
    <div>
        <h4>nombre del producto: {{this.id.title}}</h4>
        <p>descripcion: {{this.id.description}}</p>
        <p>precio: ${{this.id.price}}</p>
        <p>cantidd: {{this.quantity}}</p>
    </div>
    {{/each}}
</div>